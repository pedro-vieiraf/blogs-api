const loginValidation = require('./loginValidation');
const userValidation = require('./userValidation');
const tokenValidation = require('./tokenValidation');
const userTokenValidation = require('./userTokenValidation');

module.exports = {
  loginValidation,
  userValidation,
  tokenValidation,
  userTokenValidation,
};